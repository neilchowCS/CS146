iterate through input array and create hashmap of format { int, target - int }
check for all hashmap entries if target - int exists as a key
if found searches for index of second element of pair and returns index pair, else -1, -1